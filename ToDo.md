# ToDo
- Documentation

## Ideas
add "makeable" function into service layer to ask wheter a recipe is executable with current stock
add a "required(base)" function into probably service layer which displays all base ingredient needed to make an ingredient